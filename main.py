import re as regex  # To be used for spotting numbers throughout the text

# Find numbers in each line of the file, and add all the results up
file = open(input("Enter file name: "))
sum = int()
for line in file:
    pass
#     for number in numbers:
#         sum += int(number)
#
# # Sum
# print("Sum of inline numbers:", sum)
